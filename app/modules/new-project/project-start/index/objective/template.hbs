<div local-class="objective-container">
	<div class="next-step">
		{{!-- {{#bs-button onClick=(action "nextStep")}} --}}
		{{#bs-button class="btn-next" onClick=(action "nextStep")}}
			下一步
		{{/bs-button}}
	</div>
	<div local-class="objective-section">
		<div class="row">
			<div class="col-md-12 col-lg-12 col-sm-12">
				<select local-class="choose-area" onchange= {{action 'changeArea' value="target.value"}}>
				    {{#each regionData as |area|}}
				        <option  value={{area.id}}>{{area.name}}</option>
				    {{/each}}
				</select> {{d3-bar-line chartId="objective-chart" laterThreeChangeBg=true changeArea=(action 'changeArea')}}
			</div>
		</div>
	</div>

	<div local-class="objective-section">
		<div class="row">
			<div class="col-md-12 col-lg-12 col-sm-12">
				<p local-class="title-objective">设定目标<span>-为接下来三个月，各区域预测您期望达成目标，基于总体分析中您的分析，并参考公司指标，区域背景信息，竞争情况，做出您的目标设定。</span></p>
				<div class="row" local-class="summary">
					<div class="col-md-2 col-lg-2 col-sm-2">
						<p>公司本季度总指标</p>
						<p>3,000,000</p>
					</div>
					<div class="col-md-2 col-lg-2 col-sm-2">
						<p>您的预测指标</p>
						<p>{{totalForecast}}</p>
					</div>

				</div>

				{{!-- {{#bs-button-group value=whichMonth type="radio" local-class="btn-month-container" class="container" onChange=(action (mut whichMonth)) as |bg|}}
                    {{#bg.button local-class="btn-month" class="col-lg-4 col-md-4 col-sm-4" value="1801" }}18/01{{/bg.button}}
                    {{#bg.button local-class="btn-month" class="col-lg-4 col-md-4 col-sm-4" value="1802" }}18/02{{/bg.button}}
                    {{#bg.button local-class="btn-month" class="col-lg-4 col-md-4 col-sm-4" value="1803" }}18/03{{/bg.button}}
				{{/bs-button-group}} --}}

				{{!-- {{#if (eq whichMonth "1801")}} --}}
				<div class="table-responsive">
					<table local-class="table-container" class="table">
						<thead>
							<tr>
								<td>区域</td>
								<td>排序</td>
								<td>公司指标($)</td>
								<td>预测指标($)</td>
							</tr>
						</thead>
						<tbody>
							{{#each newRegionData as |region index|}}
								<tr>
									<td>{{region.name}} <span class="glyphicon glyphicon-list-alt" {{action 'openTips' region}}></span></td>
									<td>{{index-to-normal index}}</td>
									<td>652,000 </td>
									<td>{{input type="text" value=region.forecast dir="rtl"}} </td>
								</tr>
							{{/each}}

						</tbody>
					</table>
				</div>

			</div>

		</div>
	</div>
</div>

{{#bs-modal open=tipsModal onSubmit=(action 'toResource') onHidden=(action (mut tipsModal) false) as |modal|}}
  {{#modal.header}}
    <h4 class="modal-title">
      <span></span>{{tipsTitle}}
    </h4>
  {{/modal.header}}
  {{#modal.body}}
	{{!-- <p>请填写完成 {{emptyNotesRegion}} 的内容！</p> --}}
	<p>{{content-bound-helper tipsContent}}</p>

  {{/modal.body}}
  {{#modal.footer}}
	  {{#if isForecastEmpty}}
		  {{#bs-button onClick=(action modal.close)}}取消{{/bs-button}}
		  {{#bs-button type="success" onClick=(action modal.submit)}}下一步{{/bs-button}}
	  {{else}}
		  {{#bs-button onClick=(action modal.close)}}继续填写{{/bs-button}}
	  {{/if}}
  {{/modal.footer}}
{{/bs-modal}}
